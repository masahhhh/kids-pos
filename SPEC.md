# Kids POS 仕様書

- **文書バージョン:** 2.0
- **最終更新:** 2026-08-06
- **対象実装:** `index.html`（単一ファイル）
- **位置づけ:** 本書は実装の唯一の基準（Single Source of Truth）である。`index.html` と本書に相違がある場合、本書を正とし、実装を修正すること。

---

## 1. 概要

子ども向けの店舗ごっこ／学校行事用の簡易POSアプリ。QRコードまたはバーコードで商品をスキャンし、合計金額とおつりを計算する。

| 項目 | 内容 |
|---|---|
| 提供形態 | 単一HTMLファイル。GitHub Pages で静的配信 |
| サーバ | なし。すべてクライアント完結 |
| 通信 | CDN からのライブラリ取得のみ。商品データの外部送信は行わない |
| データ保存 | `localStorage`（端末内） |
| 想定端末 | iPhone（iOS Safari）を主、Android Chrome を副 |
| 想定利用者 | 小学生〜中学生が操作、大人が事前設定 |

### 1.1 設計方針

外部の商品データベース（JANCODE LOOKUP、Yahoo!ショッピングAPI 等）には依存しない。バーコードは「商品を識別する鍵」としてのみ使用し、商品名と価格はすべて利用者が自前で登録する。理由は次の2点である。第一に、JANコードは商品識別子であって価格情報を持たないため、外部APIから価格は取得できない。第二に、静的ホスティング環境ではAPIキーの秘匿とCORS回避ができない。

---

## 2. 外部ライブラリ

| ライブラリ | バージョン | 用途 | CDN |
|---|---|---|---|
| jsQR | 1.4.0 | 2Dコード（QR）デコード | `https://cdn.jsdelivr.net/npm/jsqr@1.4.0/dist/jsQR.js` |
| @zxing/library | 0.21.3 | 1Dコード（JAN等）デコード | `https://cdn.jsdelivr.net/npm/@zxing/library@0.21.3/umd/index.min.js` |
| qrcode-generator | 1.4.4 | 印刷用QR画像の生成 | `https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js` |

### 2.1 バージョン固定の理由

- **jsQR:** かつて使用していた `html5-qrcode`（ZXing系ラッパ）は iOS で読み取りが無言で失敗する事象が確認されたため置換した。**`html5-qrcode` への回帰は禁止。**
- **@zxing/library 0.21.3:** 最新は 0.23.0 だが、`BrowserMultiFormatOneDReader` 周辺に退行報告があるため実績のある 0.21.3 を採用。上げる場合は実機での1D読み取り確認を必須とする。
- **BarcodeDetector API は使用しない:** iOS Safari が未対応のため。将来 iOS が対応した場合は、フォールバック付きで導入を検討する。

### 2.2 起動時の健全性確認

コンソールで次の3つがすべて `"function"` を返すこと。

| 式 | 期待値 |
|---|---|
| `typeof jsQR` | `"function"` |
| `typeof ZXing.MultiFormatReader` | `"function"` |
| `typeof qrcode` | `"function"` |

---

## 3. 定数

実装では `MAX_ITEMS` 等の定数を1か所で定義し、UI表示・バリデーション・インポート処理のすべてがこれを参照する。**数値をハードコードしないこと。**

| 定数 | 値 | 説明 |
|---|---|---|
| `MAX_ITEMS` | `50` | 登録可能な商品点数の上限 |
| `MAX_NAME` | `20` | 商品名の最大文字数 |
| `MAX_PRICE` | `99999` | 価格の上限（円、整数） |
| `CODE_RE` | `/^[0-9]{8,14}$/` | バーコードとして受理する文字列 |
| `NAME_NG` | `/[\|\r\n\t]/` | 商品名に使用禁止の文字（QRペイロード区切りとの衝突回避） |
| `SCAN_SIDE` | `480` | デコード用に縮小する正方形の一辺（px） |
| `SCAN_MS` | `150` | スキャン実行間隔（ms） |
| `STALL_LIMIT` | `20` | フレーム停止をカメラ異常と判定するまでの連続回数（= 3秒） |
| `LS_P` | `kidspos.products` | 商品マスタの localStorage キー |
| `LS_S` | `kidspos.settings` | 設定の localStorage キー |

`MAX_ITEMS` の変更手順は、定数定義の1行を書き換えるのみ。UI上の「/ 50点」表記は `.max-items` クラス要素へ起動時に注入されるため、HTML側の修正は不要である。

---

## 4. データモデル

### 4.1 商品（Product）

```json
{ "name": "りんご", "price": 120, "code": "4901234567894" }
```

| フィールド | 型 | 必須 | 制約 |
|---|---|---|---|
| `name` | string | ○ | 1〜`MAX_NAME` 文字。`NAME_NG` の文字を含まない |
| `price` | integer | ○ | `0` 〜 `MAX_PRICE` の整数 |
| `code` | string | △ | 空文字可。非空なら `CODE_RE` に一致し、かつ全商品で一意 |

`code` が空の商品は、印刷QR経由でのみレジに投入できる。バーコード運用とQR運用は商品単位で混在可能である。

### 4.2 設定（Settings）

```json
{ "qrSize": 28 }
```

| フィールド | 型 | 取りうる値 | 説明 |
|---|---|---|---|
| `qrSize` | integer | `28` / `40` | 印刷するQRコードの一辺（mm） |

### 4.3 カート（Cart）

セッション内のみ保持し、`localStorage` には保存しない。会計完了時に破棄する。要素は `{ name, price, qty }` であり、`name` と `price` が完全一致する場合のみ `qty` を加算して集約する。

---

## 5. コード仕様

### 5.1 印刷QRのペイロード

```
KP1|商品名|税込価格
```

| 位置 | 内容 | 備考 |
|---|---|---|
| 1 | `KP1` | 固定のマジックワード兼バージョン |
| 2 | 商品名 | `MAX_NAME` 文字以内。`|` を含めてはならない |
| 3 | 価格 | 半角整数。円 |

QRは UTF-8 バイトモード、誤り訂正レベル `M` で生成する。ペイロードに価格を埋め込む設計であるため、**QRラベルは価格改定のたびに再印刷が必要**である。

### 5.2 対応する1Dバーコード

| 形式 | 用途 |
|---|---|
| EAN-13 / EAN-8 | 日本のJANコード |
| UPC-A / UPC-E | 北米商品 |
| CODE-128 / CODE-39 | 自作ラベル |
| ITF | 集合包装用 |

デコード時は `TRY_HARDER` ヒントを有効にする。1Dは低品質画像での失敗が多く、このヒントなしでは実用的な成功率に達しない。

### 5.3 読み取り結果の振り分け

デコードされた文字列は次の順で判定する。

| 条件 | 処理 |
|---|---|
| `KP1|` で始まる | ペイロードを分解し、商品名と価格を直接カートへ追加 |
| `CODE_RE` に一致 | 商品マスタを `code` で検索。ヒットすれば追加、未登録なら警告音とトースト |
| 上記以外 | 「対応していないコード」として拒否 |

同一文字列の連続検出は 1500ms 以内であれば無視する（チャタリング防止）。

---

## 6. スキャナ仕様

### 6.1 プレビュー

`#reader` は `aspect-ratio:1/1` の正方形とし、`video` は `object-fit:cover` で中央を切り出して表示する。iPhone の縦長プレビューは対象物が小さく写り読み取り率を下げるため、正方形固定とした。

### 6.2 読み取り範囲（ROI）

デコードは `video` の中央正方形を `SCAN_SIDE` 四方へ縮小した canvas に対して行う。この canvas 上でさらに2種類のROIを使い分ける。

| コード種別 | ROI | ガイド枠 | 実行頻度 |
|---|---|---|---|
| QR（jsQR） | 正方形の全域 | 白の実線（`#reader::after`） | 毎フレーム |
| 1D（ZXing） | 上から30%の位置、高さ40%の横長帯 | 黄の破線（`#reader::before`） | 2フレームに1回 |

1Dを正方形ROIのまま処理すると、横長のバーコードは左右が切れて読み取り率が著しく低下する。**この横長帯の切り出しを削除してはならない。** 1D処理を毎フレーム実行すると iPhone でプレビューがカクつくため間引いている。

### 6.3 輝度変換

ZXing の `RGBLuminanceSource` は `Uint8ClampedArray` を「輝度そのもの」として解釈するため、`ImageData` の RGBA を事前に単一チャンネルへ変換する必要がある。変換式は ZXing 内部の緑重視平均に合わせ $Y=(306R+601G+117B)/1024$ の整数近似を用いる。

| 記号 | 説明 |
|---|---|
| $Y$ | 出力する輝度値（0〜255） |
| $R,G,B$ | 各画素の赤・緑・青成分（0〜255） |

RGBA配列をそのまま渡すと、幅が4倍に解釈され一切デコードできない。

### 6.4 カメラ制約

`getUserMedia` に渡す制約は `facingMode:{ideal:'environment'}`、`width:{ideal:1280}`、`height:{ideal:1280}` とする。読み取り率が不足する場合の調整順序は、`SCAN_SIDE` を 480 から 720 へ引き上げ、次に1Dの間引きを解除し、最後に解像度制約を上げる。

---

## 7. iOS 固有の回避策（削除禁止）

本節の実装は、コードだけを読むと冗長に見えるが、いずれも実機で再現した不具合への対処である。理由を確認せずに削除・整理してはならない。

| 実装 | 対処する事象 |
|---|---|
| `resumeVideo()` をダイアログ閉鎖後に呼ぶ | モーダル表示中に `video` が停止し、閉じても直前フレームの静止画のまま固まる |
| 自前モーダル（native `confirm` 不使用） | ネイティブダイアログが `video` を強制停止させる。「全部消す」で発生した不具合の根本原因 |
| `STALL_LIMIT` によるフレーム停止検知と再接続 | `video.currentTime` が進まなくなる（黒画面／フリーズ）事象からの自動復帰 |
| `visibilitychange` / `pageshow` / `focus` での再開 | バックグラウンド復帰時に映像が戻らない |
| `unlockAudio()` を初回タップで実行 | ユーザー操作なしでは `AudioContext` が `suspended` のままで無音 |
| `unlockAudio()` をカメラ起動直後に**再度**実行 | マイク／カメラ使用でオーディオセッションが切り替わり、効果音が鳴らなくなる |
| `navigator.audioSession.type = 'playback'` | 同上。対応ブラウザでのみ効く保険であり、単独では不十分 |
| `playsinline` / `muted` 属性 | 指定しないと全画面再生に切り替わりスキャン不能 |

---

## 8. 画面仕様

| 画面ID | 名称 | 主な機能 |
|---|---|---|
| `scr-home` | ホーム | 各画面への遷移、登録点数の表示 |
| `scr-products` | 商品リスト | 登録済み商品の一覧、編集への遷移 |
| `scr-edit` | 商品のとうろく | 商品名・価格・バーコードの入力、保存、削除 |
| `scr-print` | QRコード印刷 | ラベルのプレビュー、サイズ切替、印刷実行 |
| `scr-cart` | レジ | スキャン、カート表示、合計、全消去 |
| `scr-pay` | おかいけい | 預り金入力、おつり計算、完了 |
| `scr-data` | データ | 書き出し・読みこみ |

`#reader` 要素は DOM 上に1つだけ存在し、使用する画面のホスト要素へ `appendChild` で移動させる。複数生成するとカメラストリームが競合する。

### 8.1 商品登録の入力検証

エラー時は警告音を鳴らし、トーストでメッセージを表示する。保存は行わない。

| 条件 | メッセージ |
|---|---|
| 商品名が空 | 商品名をいれてください |
| 商品名が `MAX_NAME` 超過 | 商品名は20文字までです |
| 商品名に禁止文字 | 商品名に使えない記号（\| 改行）があります |
| 価格が範囲外／非整数 | ねだんは0〜99999の整数でいれてください |
| コードが `CODE_RE` 不一致 | バーコードは数字8〜14桁です |
| コードが他商品と重複 | そのバーコードは「〇〇」に登録ずみです |
| 新規登録かつ `MAX_ITEMS` 到達 | 商品は50点までです |

### 8.2 効果音

Web Audio API の `OscillatorNode` で生成する。音声ファイルは持たない。

| 音 | 用途 | 内容 |
|---|---|---|
| `beepOk` | 読み取り成功 | 1660Hz を 90ms |
| `beepNg` | エラー | 220Hz → 180Hz の低音2連 |
| `beepDone` | 会計完了 | 880 → 1174 → 1568Hz の上昇3音 |

---

## 9. データ入出力

### 9.1 書き出し形式

```json
{
  "app": "kidspos",
  "format": 2,
  "exportedAt": "2026-08-06T00:00:00.000Z",
  "products": [
    { "name": "りんご", "price": 120, "code": "4901234567894" },
    { "name": "みかん", "price": 80,  "code": "" }
  ]
}
```

| フィールド | 説明 |
|---|---|
| `app` | 固定値 `kidspos`。他アプリのJSON誤読み込みを防ぐ |
| `format` | 形式バージョン。現行は `2` |
| `exportedAt` | ISO 8601 形式の書き出し日時 |
| `products` | 商品配列 |

出力手段はファイルダウンロード（ファイル名 `kidspos-YYYYMMDD.json`）と、クリップボードへのテキストコピーの2系統。クリップボードAPIが使えない環境では、貼り付け欄にテキストを出力してフォールバックする。

### 9.2 形式バージョン

| version | 差分 |
|---|---|
| `1` | `products` の要素は `{name, price}` のみ |
| `2` | `code` フィールドを追加 |

### 9.3 読み込み時の正規化

読み込みは常に**全置換**であり、追記はしない。実行前に確認モーダルで件数を提示する。

| 検査 | 挙動 |
|---|---|
| `app !== "kidspos"` | 拒否。「Kids POS のデータではありません」 |
| `format` が 1 でも 2 でもない | 拒否。「対応していない形式です」 |
| `format === 1` | 各商品の `code` を空文字として取り込む（下位互換） |
| 商品名が空／不正 | その1件をスキップ |
| 価格が範囲外 | 0 または `MAX_PRICE` に丸める |
| `code` が `CODE_RE` 不一致 | 空文字に置換 |
| `code` が重複 | 後勝ちを空文字に置換し、商品自体は残す |
| 件数が `MAX_ITEMS` 超過 | 先頭 `MAX_ITEMS` 件のみ採用 |
| 採用件数が0 | 拒否。「読みこめる商品がありませんでした」 |

---

## 10. 印刷仕様

商品ごとにQR画像・商品名・価格を含むラベルを生成し、`flex-wrap` で並べる。`@media print` でヘッダーと操作系（`.noprint`）を非表示にし、`page-break-inside:avoid` でラベルの分断を防ぐ。

| サイズ設定 | QR実寸 | 用途 |
|---|---|---|
| 28mm | 28mm 角 | 小さい商品、印刷枚数を節約したい場合 |
| 40mm | 40mm 角 | 読み取り優先。iPhone のオートフォーカスが合いやすい |

読み取り不良が発生する場合は 40mm を選択する。

---

## 11. 既知の制約

JANコードの読み取りは印刷QRより難易度が高い。バーコードの物理サイズが小さく、バーの細さがカメラの解像限界とオートフォーカス精度に直結するためである。運用上は、バーコードが画面幅の6〜8割を占める距離まで近づけ、一度20cm程度離してから寄せるとフォーカスが合いやすい。安定性を最優先する場面では、印刷QR運用を主軸にすることを推奨する。

その他、`localStorage` は Safari のプライベートブラウズやサイトデータ削除で消失するため、行事前には必ずデータ書き出しでバックアップを取ること。またオフラインでは CDN からライブラリを取得できず起動しない（Service Worker によるキャッシュは未実装）。

---

## 12. 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | — | 初版。QR（jsQR）によるレジ機能、商品マスタ、印刷、データ入出力 |
| 1.1 | — | 商品点数上限を 30 から 50 へ変更。`MAX_ITEMS` 定数化 |
| 1.2 | — | カメラプレビューを正方形化。中央ROI切り出しを導入 |
| 2.0 | 2026-08-06 | 1Dバーコード対応（@zxing/library 併用）。`code` フィールド追加。書き出し形式を `format:2` へ。1D用ROI（横長帯）とガイド枠を追加。iOS回避策を明文化 |
