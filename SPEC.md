# Kids POS 仕様書

| 項目 | 内容 |
| --- | --- |
| ドキュメント名 | Kids POS（おみせやさんレジ）仕様書 |
| バージョン | v2.1 |
| 最終更新 | 2026-08-06 |
| 対象実装 | `index.html`（単一ファイル） |
| 状態 | 実機試用中 |
| 位置づけ | 本書は実装の唯一の基準とする。実装と本書に相違がある場合は本書を正とし、実装を修正すること |

---

## 1. 目的とスコープ

子どもの「おみせやさんごっこ」で使用する、コード読み取り式の簡易レジアプリケーションである。商品に貼付したQRコードに税込価格を直接埋め込む方式を基本とし、市販商品のバーコード（JAN等）を自前の商品マスタと紐付けて使用することもできる。読み取った内容から合計金額とお釣りを算出する。

### 1.1 スコープ内

商品マスタ（名称・税込価格・バーコード）の登録、編集、削除、一覧表示。商品QRコードの生成とA4普通紙への印刷。カメラによるQRコードおよびバーコードの読み取りとカート投入。合計金額の算出、預り金入力、お釣り算出。効果音・音量の設定。商品データの書き出しと読み込み。

### 1.2 スコープ外

売上履歴の記録・集計、在庫管理、複数端末間のデータ同期は対象外とする。消費税計算はQRに税込価格を埋め込む方式のためアプリ内では行わない。会員管理、値引き、返品処理、およびサーバサイド処理全般（バックエンドを持たない）も対象外である。

外部の商品データベース（JANCODE LOOKUP、Yahoo!ショッピングAPI 等）にも依存しない。バーコードは商品を識別する鍵としてのみ使用し、商品名と価格は全て利用者が自前で登録する。理由は2点ある。第一に、JANコードは商品識別子であって価格情報を持たないため、外部APIから価格は取得できない。第二に、静的ホスティング環境ではAPIキーの秘匿とCORS回避ができない。

---

## 2. 動作環境

| 区分 | 要件 |
| --- | --- |
| 配信方式 | GitHub Pages（Publicリポジトリ、HTTPS自動付与） |
| 想定端末 | iPadOS / iOS の Safari（主）、Android Chrome、PCブラウザ（副） |
| 必須条件 | セキュアコンテキスト（HTTPS）。カメラAPIはHTTP環境では動作しない |
| 通信 | 初回読み込み時にCDNへの接続が必要。読み込み後の会計処理は端末内で完結する |
| 権限 | カメラ権限の許可が必要（レジ機能およびバーコード登録時のみ） |

Service Workerは導入していないため、完全なオフライン動作は保証しない。イベント当日は事前にページを開いた状態を維持する運用とする。

---

## 3. システム構成

単一の `index.html` に、HTML・CSS・JavaScriptを全て内包する静的Webアプリケーションである。フレームワークは使用しない（Vanilla JS）。

| 構成要素 | 採用技術 | 供給元 |
| --- | --- | --- |
| QRコード生成 | qrcode-generator 1.4.4 | `https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js` |
| QRコード読取 | jsQR 1.4.0 | `https://cdn.jsdelivr.net/npm/jsqr@1.4.0/dist/jsQR.js` |
| バーコード読取 | @zxing/library 0.21.3 | `https://cdn.jsdelivr.net/npm/@zxing/library@0.21.3/umd/index.min.js` |
| カメラ制御 | MediaDevices.getUserMedia（標準API） | ブラウザ組込 |
| 効果音 | Web Audio API（OscillatorNode） | ブラウザ組込 |
| データ保存 | localStorage | ブラウザ組込 |

### 3.1 読取ライブラリの選定経緯

当初は html5-qrcode（ZXing系）を採用したが、iOS環境で映像は表示されるにもかかわらずデコードが無言で失敗する事象が発生した。診断ページによる切り分けの結果、同一のカメラストリームに対して jsQR は正常にデコードできることを確認したため、jsQR＋自前フレーム処理へ全面移行した。**html5-qrcode への回帰は禁止とする。**

バーコード（1D）対応にあたっては、jsQR が2D専用であるため @zxing/library を併用する。ZXingの最新版は 0.23.0 であるが、`BrowserMultiFormatOneDReader` 周辺に退行報告があるため実績のある 0.21.3 を採用した。バージョンを上げる場合は実機での1D読み取り確認を必須とする。なお `BarcodeDetector` API は iOS Safari が未対応のため使用しない。

### 3.2 起動時の健全性確認

コンソールで次の3つが全て `"function"` を返すこと。いずれかが失敗する場合はCDNからの取得に失敗している。

| 式 | 期待値 |
| --- | --- |
| `typeof jsQR` | `"function"` |
| `typeof ZXing.MultiFormatReader` | `"function"` |
| `typeof qrcode` | `"function"` |

---

## 4. 定数

実装では定数を1か所で定義し、UI表示・入力検証・読み込み処理の全てがこれを参照する。数値をハードコードしてはならない。上限値の変更は定数定義の1行と本表の修正のみで完結する。

| 定数 | 値 | 説明 |
| --- | --- | --- |
| `MAX_ITEMS` | `50` | 登録可能な商品点数の上限 |
| `MAX_NAME` | `20` | 商品名の最大文字数 |
| `MAX_PRICE` | `99999` | 価格の上限（円、整数） |
| `MAX_RECV` | `9999999` | 預り金の上限（7桁） |
| `CODE_RE` | `/^[0-9]{8,14}$/` | バーコードとして受理する文字列 |
| `NAME_NG` | `/[\|\r\n\t]/` | 商品名に使用禁止の文字 |
| `SCAN_MS` | `150` | 走査間隔（ms） |
| `DEDUPE_MS` | `2000` | 同一コードの重複抑止時間（ms） |
| `STALL_LIMIT` | `20` | フレーム停止をカメラ異常と判定する連続回数（= 約3秒） |
| `SCAN_SIDE` | `640` | デコード用に縮小する正方形の一辺（px） |
| `LS_P` | `kidspos.products` | 商品マスタの localStorage キー |
| `LS_S` | `kidspos.settings` | 設定の localStorage キー |

UI上の「/ 50点」等の表記は `.max-items` および `.max-name` クラス要素へ起動時に注入されるため、HTML側の修正は不要である。

---

## 5. データ仕様

### 5.1 商品（localStorageキー `kidspos.products`）

JSON配列として保存する。要素の構造は次のとおり。

| フィールド | 型 | 必須 | 説明 |
| --- | --- | --- | --- |
| `id` | string | ○ | 登録時刻を36進数化した内部ID。表示せず、編集対象の特定に用いる |
| `name` | string | ○ | 商品名。1〜`MAX_NAME` 文字。`|` および改行は使用不可 |
| `price` | number | ○ | 税込価格。0〜`MAX_PRICE` の整数 |
| `code` | string | △ | バーコード。空文字可。非空なら `CODE_RE` に一致し、かつ全商品で一意 |

登録上限は `MAX_ITEMS`（50点）とする。`code` が空の商品は印刷QR経由でのみレジに投入できる。バーコード運用とQR運用は商品単位で混在可能である。

### 5.2 設定（localStorageキー `kidspos.settings`）

| フィールド | 型 | 既定値 | 説明 |
| --- | --- | --- | --- |
| `tone` | string | `pi` | 効果音の種類。`pi` / `pipo` / `pon` / `charin` / `bell` / `none` |
| `vol` | string | `mid` | 音量。`low` / `mid` / `high` |
| `vibration` | boolean | `true` | 振動の有効可否（iOSでは動作しない） |
| `qrSize` | number | `28` | 印刷するQRコードの一辺（mm）。`28` / `40` |
| `showQr` | boolean | `false` | 商品一覧でのQRサムネイル表示 |
| `sort` | string | `reg` | 一覧の並び順。`reg` / `name` / `price` |

### 5.3 カート（メモリ上のみ、永続化しない）

| フィールド | 型 | 説明 |
| --- | --- | --- |
| `name` | string | 商品名（QRまたは商品マスタから取得した値） |
| `price` | number | 税込価格 |
| `qty` | number | 数量。0以下になった時点で行を削除する |

同一行と判定する条件は「名称と価格の両方が一致すること」とする。商品IDはQRに含めないため、カートは商品マスタに依存しない。会計完了時にカートは破棄される。

---

## 6. コード仕様

### 6.1 QRコード

| 項目 | 仕様 |
| --- | --- |
| データ形式 | `KP1\|商品名\|税込価格`（区切り文字はパイプ） |
| 例 | `KP1\|りんご\|120` |
| 文字コード | UTF-8（Byteモード。ライブラリの既定はマルチバイト非対応のため変換関数を明示指定） |
| 誤り訂正レベル | M |
| 型番 | 自動判定（データ長に応じて決定） |
| 描画形式 | SVG（`cellSize` 4、`margin` 8、スケーラブル出力） |
| 印刷サイズ | 28mm角または40mm角（設定で選択） |

読み取り時は、要素数が3であること、先頭が `KP1` であること、価格が0以上の整数として解釈できることの3点を検証し、いずれかを満たさない場合はエラー音とともに「このQRコードは使えません」と表示する。

商品名が長いほど型番が上がり1マスが細くなるため、読み取り精度を優先する場合は短い名称を推奨する旨を登録画面に注記する。QRには価格を埋め込むため、**価格改定のたびにラベルの再印刷が必要**である。

### 6.2 バーコード

| 形式 | 用途 |
| --- | --- |
| EAN-13 / EAN-8 | 日本のJANコード |
| UPC-A / UPC-E | 北米商品 |
| CODE-128 / CODE-39 | 自作ラベル |
| ITF | 集合包装用 |

デコード時は `TRY_HARDER` ヒントを有効にする。1Dは低品質画像での失敗が多く、このヒントなしでは実用的な成功率に達しない。

### 6.3 読み取り結果の振り分け

デコードされた文字列は次の順で判定する。

| 条件 | 処理 |
| --- | --- |
| `KP1\|` で始まる | ペイロードを分解し、商品名と価格を直接カートへ追加 |
| `CODE_RE` に一致 | 商品マスタを `code` で検索。該当すれば追加、未登録なら警告音と「未登録のバーコードです」を表示 |
| 上記以外 | 「このQRコードは使えません」として拒否 |

商品登録画面から起動した読み取りモードでは、`CODE_RE` に一致する文字列のみを受理し、バーコード入力欄へ反映してカメラを停止する。

---

## 7. 画面仕様

全体は単一ページ内のセクション切り替え（SPA方式）で構成し、ヘッダーには常時「ホーム」ボタンを配置する。

| 画面ID | 名称 | 主な機能 |
| --- | --- | --- |
| `scr-home` | ホーム | 各機能への遷移メニュー、登録点数の表示 |
| `scr-list` | 商品一覧 | 一覧表示、並び替え、QRサムネイル表示切替、行タップで編集へ |
| `scr-products` | 商品の登録 | 新規登録、編集、削除、バーコード読取、入力検証 |
| `scr-print` | QRコードの印刷 | サイズ切替、印刷プレビュー、印刷実行 |
| `scr-register` | レジ | カメラ読取、カート操作、小計表示 |
| `scr-checkout` | お会計 | 預り金入力（キーパッド・定額ボタン）、お釣り表示 |
| `scr-settings` | 設定 | 効果音、音量、振動、データ全削除 |
| `scr-data` | データの保存・読込 | 書き出し、読み込み |

`#reader` 要素はDOM上に1つだけ存在し、使用する画面のスロットへ `appendChild` で移動させる。複数生成するとカメラストリームが競合する。

### 7.1 商品一覧

並び替えは登録順、名前順（日本語ロケール比較）、価格順の3種類を提供する。QRサムネイルは既定で非表示とし、ボタンで切り替える。表示件数は「登録数 n / 50点」の形式で示す。バーコード列には登録済みコードを表示し、未登録の商品は「—」とする。

### 7.2 商品の登録

商品名、価格、バーコードを入力する。バーコード欄には読取ボタンと消去ボタンを併設し、読取ボタン押下時のみ画面内にカメラを展開する。

入力検証でエラーとなった場合はエラー音を鳴らし、トーストでメッセージを表示して保存を行わない。

| 条件 | メッセージ |
| --- | --- |
| 商品名が空 | 商品名を入力してください |
| 商品名が `MAX_NAME` 超過 | 商品名は20文字以内で入力してください |
| 商品名に禁止文字 | 商品名に使用できない記号（\|・改行）が含まれています |
| 価格が範囲外または非整数 | 価格は0〜99999の整数で入力してください |
| コードが `CODE_RE` 不一致 | バーコードは数字8〜14桁で入力してください |
| コードが他商品と重複 | そのバーコードは「〇〇」に登録済みです |
| 新規登録かつ `MAX_ITEMS` 到達 | 商品の登録は50点までです |

### 7.3 レジ

カメラ映像の下に直近の読取結果を表示し、その下にカート、合計、点数、操作ボタン（一覧から選ぶ／全て消去／お会計）を配置する。「一覧から選ぶ」は、コードが読めない場合の代替入力手段として商品マスタからのボタン選択を提供する。カートが空の間は「お会計」ボタンを無効化する。

### 7.4 お会計

預り金は3桁キーパッド（0〜9、00、C）と定額ボタン（100 / 500 / 1000 / 5000 / 10000／同額）で入力する。入力桁数は7桁までに制限する。預り金が合計未満の場合は「お金が足りません」と表示し、「会計を終了」ボタンを無効化する。会計終了後はカートを空にし、レジ画面へ戻って読み取りを再開する。

---

## 8. 計算仕様

合計金額は、カート内の各行について税込単価と数量の積を合算した $T=\sum_{i=1}^{n} p_i q_i$ で求める。お釣りは預り金から合計を差し引いた $C = R - T$ とし、$C<0$ の場合は会計を確定させない。点数表示は $Q=\sum_{i=1}^{n} q_i$ とする。

| 変数 | 説明 |
| --- | --- |
| $n$ | カート内の明細行数 |
| $p_i$ | 第 $i$ 行の税込単価（円、整数） |
| $q_i$ | 第 $i$ 行の数量（正の整数） |
| $T$ | 合計金額（円） |
| $R$ | 預り金（円、0〜`MAX_RECV`） |
| $C$ | お釣り（円） |
| $Q$ | 合計点数 |

金額は全て整数で扱い、丸め処理および税計算は行わない。

---

## 9. 読み取り処理仕様

| 項目 | 仕様 |
| --- | --- |
| カメラ取得 | `facingMode: {ideal:'environment'}`、解像度は1920×1080を要求（`ideal`） |
| 走査間隔 | 150ms（`setTimeout` の連鎖） |
| 前処理 | video要素の中央正方形を canvas へ `SCAN_SIDE` 四方で描画 |
| QRデコード | jsQRに `inversionAttempts: 'attemptBoth'` を指定 |
| 1Dデコード | ZXing `MultiFormatReader`。2フレームに1回実行 |
| 重複抑止 | 同一文字列は `DEDUPE_MS`（2000ms）以内の再検出を無視する |
| 停止条件 | レジ・商品登録以外の画面への遷移、会計画面への移行、タブの非表示化 |

### 9.1 プレビューと読み取り範囲

`#reader` は `aspect-ratio:1/1` の正方形とし、`video` は `object-fit:cover` で中央を切り出して表示する。iPhoneの縦長プレビューは対象物が小さく写り読み取り率を下げるため、正方形固定とした。

デコードはこの中央正方形を `SCAN_SIDE` 四方へ縮小したcanvasに対して行い、canvas上でさらに2種類の範囲を使い分ける。

| コード種別 | 読み取り範囲 | ガイド枠 | 実行頻度 |
| --- | --- | --- | --- |
| QR（jsQR） | 正方形の全域 | 白の実線（`#reader::after`） | 毎フレーム |
| 1D（ZXing） | 上から30%の位置、高さ40%の横長帯 | 黄の破線（`#reader::before`） | 2フレームに1回 |

1Dを正方形範囲のまま処理すると、横長のバーコードは左右が切れて読み取り率が著しく低下する。**この横長帯の切り出しを削除してはならない。** また1D処理を毎フレーム実行するとiPhoneでプレビューが不安定になるため間引いている。

### 9.2 輝度変換

ZXingの `RGBLuminanceSource` は `Uint8ClampedArray` を輝度そのものとして解釈するため、`ImageData` のRGBAを事前に単一チャンネルへ変換する必要がある。変換式はZXing内部の緑重視平均に合わせ $Y=(306R+601G+117B)/1024$ の整数近似を用いる。

| 記号 | 説明 |
| --- | --- |
| $Y$ | 出力する輝度値（0〜255） |
| $R,G,B$ | 各画素の赤・緑・青成分（0〜255） |

RGBA配列をそのまま渡すと幅が4倍に解釈され、一切デコードできない。

### 9.3 フリーズ復旧

iOSでは `confirm` / `alert` の表示によりvideo要素が一時停止し、閉じても自動再開しない。この結果、直前のフレームを描画し続けて静止画状態となるため、以下の三重の復旧機構を実装する。

第一に、ダイアログを閉じた直後に再生を再開する（`resumeVideo()`）。第二に、video要素の `pause` / `ended` イベントを監視し、スキャン中であれば再開または再接続する。第三に、走査ごとに `currentTime` の進行を監視し、約3秒（150ms × 20回）進まない場合はカメラを停止して再取得する。

またタブが再表示された際、レジ画面または商品登録画面が表示中であればスキャンを自動再開する。

なお v2.0 以降は確認ダイアログをHTMLベースの自前モーダルに置き換えており、ネイティブダイアログは使用しない。上記の復旧機構は他要因（バックグラウンド遷移、電話着信等）への保険として維持する。

---

## 10. 効果音仕様

Web Audio APIのオシレータで合成する。音源ファイルは持たない。

| 種類 | 内容 |
| --- | --- |
| `pi` | 1200Hz、0.10秒、正弦波 |
| `pipo` | 988Hz→1319Hzの2音 |
| `pon` | 880Hz、0.30秒、三角波 |
| `charin` | 2093 / 3136 / 2637Hzの重ね合わせ（レジ風） |
| `bell` | 1568Hz＋3136Hz、減衰長め（チーン） |
| `none` | 無音 |
| エラー音 | 300Hz、0.30秒、鋸歯状波（共通・設定不可） |

音量は小0.06、中0.15、大0.30のゲイン値で切り替える。効果音は読み取り成功、一覧からの選択、保存、会計終了で鳴らし、エラー音は入力検証エラーおよび読み取り失敗で鳴らす。

### 10.1 iOSの無音対策

iOSではユーザー操作前のAudioContextが停止状態となり、またカメラ起動時に音声セッションが切り替わって無音化する事象がある。対策として、初回タップ時にAudioContextを生成・再開し無音バッファを再生してロックを解除するとともに、対応環境では `navigator.audioSession.type` に `playback` を宣言する。**カメラ起動直後にも同処理を再実行する。** 本体が消音（サイレント）状態の場合は鳴らないため、設定画面に注意書きを掲示する。

---

## 11. 印刷仕様

| 項目 | 28mm設定 | 40mm設定 |
| --- | --- | --- |
| 用紙 | A4普通紙、縦向き、余白10mm | 同左 |
| レイアウト | 3列グリッド、1セル63.3mm × 55mm | 2列グリッド、1セル95mm × 68mm |
| 1ページあたり | 15点（3列 × 5行） | 8点（2列 × 4行） |
| QR実寸 | 28mm角 | 40mm角 |

セル内容はQRコード、商品名、価格、「税込」表記、切り取り用の破線とする。プレビューは画面上で45%に縮小表示し、印刷時は等倍とする。切り取り線を紙面に出力するには、印刷ダイアログで「背景のグラフィック」を有効にする必要がある。

読み取り不良が発生する場合は40mm設定を選択する。QRの物理サイズはiPhoneのオートフォーカス精度に直結するため、読み取り率の改善に最も効果が大きい。

---

## 12. データの書き出しと読み込み

### 12.1 形式

```json
{
  "app": "kidspos",
  "format": 2,
  "exportedAt": "2026-08-06T00:00:00.000Z",
  "products": [
    { "name": "りんご", "price": 120, "code": "4901234567894" },
    { "name": "みかん", "price": 80,  "code": "" }
  ]
}
```

| フィールド | 説明 |
| --- | --- |
| `app` | 固定値 `kidspos`。他アプリのJSON誤読み込みを防ぐ |
| `format` | 形式バージョン。現行は `2` |
| `exportedAt` | ISO 8601 形式の書き出し日時 |
| `products` | 商品配列。`id` は端末固有のため出力せず、読み込み時に再採番する |

出力手段はファイルダウンロード（ファイル名 `kidspos-YYYYMMDD.json`）と、クリップボードへのテキストコピーの2系統。クリップボードAPIが使用できない環境では、貼り付け欄にテキストを出力してフォールバックする。

| version | 差分 |
| --- | --- |
| `1` | `products` の要素は `{name, price}` のみ |
| `2` | `code` フィールドを追加 |

### 12.2 読み込み時の正規化

読み込みは常に全置換であり、追記はしない。実行前に確認モーダルで件数を提示する。

| 検査 | 挙動 |
| --- | --- |
| `app !== "kidspos"` | 拒否。「Kids POS のデータではありません」 |
| `format` が 1 でも 2 でもない | 拒否。「対応していない形式です」 |
| `format === 1` | 各商品の `code` を空文字として取り込む（下位互換） |
| 商品名が空または不正 | その1件をスキップ |
| 価格が範囲外 | 0 または `MAX_PRICE` に丸める |
| `code` が `CODE_RE` 不一致 | 空文字に置換 |
| `code` が重複 | 後勝ちを空文字に置換し、商品自体は残す |
| 件数が `MAX_ITEMS` 超過 | 先頭 `MAX_ITEMS` 件のみ採用 |
| 採用件数が0 | 拒否。「読み込める商品がありませんでした」 |

---

## 13. 非機能要件・制約事項

ソースコードはGitHub Pagesの仕様上公開されるため、APIキー等の機密情報および個人情報を一切含めない。商品データは端末のlocalStorageにのみ保存され、外部へ送信されない。ブラウザのデータ削除やプライベートブラウズにより消失するため、行事前には必ずデータの書き出しでバックアップを取ること。

対象年齢は小学校高学年を想定し、画面表記は常用漢字を中心とした平易な日本語とする。振動はiOS / iPadOSのSafariが `navigator.vibrate` を実装していないため動作しない。設定項目は他環境向けに残置する。商品名に `|` を含めることはできない（QRの区切り文字と衝突するため）。

バーコードの読み取りは印刷QRより難易度が高い。バーの細さがカメラの解像限界とオートフォーカス精度に直結するためである。運用上は、バーコードが画面幅の6〜8割を占める距離まで近づけ、一度20cm程度離してから寄せるとフォーカスが合いやすい。安定性を最優先する場面では印刷QR運用を主軸にすることを推奨する。

読み取り率が不足する場合の調整順序は、`SCAN_SIDE` を640から720以上へ引き上げ、次に1Dデコードの間引き（2フレームに1回）を解除し、最後に解像度制約を上げる、とする。

---

## 14. 削除禁止の実装

本節の実装は、コードだけを読むと冗長に見えるが、いずれも実機で再現した不具合への対処である。理由を確認せずに削除・整理してはならない。

| 実装 | 対処する事象 |
| --- | --- |
| 自前モーダル（ネイティブ `confirm` 不使用） | ネイティブダイアログが `video` を強制停止させる。「全て消去」で発生した不具合の根本原因 |
| `resumeVideo()` をダイアログ閉鎖後に呼ぶ | モーダル表示中に `video` が停止し、閉じても直前フレームの静止画のまま固まる |
| `STALL_LIMIT` によるフレーム停止検知と再接続 | `currentTime` が進まなくなる（黒画面・フリーズ）事象からの自動復帰 |
| `pause` / `ended` イベント監視 | 同上。イベント駆動での早期復帰 |
| `visibilitychange` / `pageshow` / `focus` での再開 | バックグラウンド復帰時に映像が戻らない |
| `unlockAudio()` を初回タップで実行 | ユーザー操作なしでは `AudioContext` が `suspended` のままで無音 |
| `unlockAudio()` をカメラ起動直後に再度実行 | カメラ使用でオーディオセッションが切り替わり効果音が鳴らなくなる |
| `navigator.audioSession.type = 'playback'` | 同上。対応ブラウザでのみ効く保険であり、単独では不十分 |
| `playsinline` / `muted` 属性 | 指定しないと全画面再生に切り替わりスキャン不能 |
| 1D用の横長帯切り出し | 正方形範囲ではバーコードの左右が切れて読み取れない |
| RGBAから輝度への事前変換 | ZXingは輝度配列を要求し、RGBAを渡すと一切デコードできない |

---

## 15. 試験項目（受け入れ基準）

| # | 確認内容 | 期待結果 |
| --- | --- | --- |
| 1 | 商品を登録する | 一覧・印刷プレビューにQR付きで表示される |
| 2 | 20文字超・空文字・負の価格を入力する | エラーメッセージが表示され登録されない |
| 3 | 50点登録後にさらに登録する | 上限エラーが表示される |
| 4 | 印刷プレビューを実行する | A4に規定の列数で配置され、QRが欠落しない |
| 5 | 印刷したQRを読み取る | カートに追加され、効果音が鳴る |
| 6 | 同じコードを連続してかざす | 2秒以内は重複追加されない |
| 7 | 「全て消去」で実行／取消を押す | 映像が静止せず読み取りを継続できる |
| 8 | 他アプリへ移動して戻る | レジ画面ならカメラが自動再開する |
| 9 | 預り金が合計未満 | 「お金が足りません」と表示され確定できない |
| 10 | 会計を完了する | お釣りが表示され、カートが空になり読取が再開する |
| 11 | 商品にバーコードを登録し読み取る | 該当商品がカートに追加される |
| 12 | 未登録のバーコードを読み取る | 「未登録のバーコードです」と表示され追加されない |
| 13 | 同一バーコードを2商品に登録する | 重複エラーが表示され登録されない |
| 14 | 書き出したファイルを読み込む | 確認後に全置換され、件数が一致する |
| 15 | `format:1` のファイルを読み込む | 全商品の `code` が空として取り込まれる |
| 16 | 効果音を各種類に切り替える | 選択時に試聴音が鳴り、レジでも同じ音が鳴る |
| 17 | QRサイズを40mmに切り替える | プレビューが2列×4行に変わり、印刷結果も一致する |

---

## 16. 今後の拡張候補

売上履歴の記録と当日集計の表示、PWA化（Service Worker導入によるオフライン起動とCDN依存の解消）、複数店舗分の商品セット切り替え、印刷ラベルへのバーコード出力（自作CODE-128の生成）が候補として残っている。

---

## 17. 変更履歴

| 版 | 日付 | 内容 |
| --- | --- | --- |
| v1.0 | — | 初版。QR読取によるレジ機能、商品マスタ、印刷、効果音設定 |
| v1.1 | 2026-08-02 | 読取ライブラリを html5-qrcode から jsQR へ全面移行。フリーズ復旧機構を三重化 |
| v2.0 | 2026-08-06 | 商品点数上限を30から50へ変更。1Dバーコード対応（@zxing/library 併用）、`code` フィールド追加、書き出し形式 `format:2`、正方形プレビューと読み取り範囲の二段構成 |
| v2.1 | 2026-08-06 | v1.1 の記述（効果音6種・音量3段・振動設定・商品ID・並び替え・QRサムネイル・キーパッド会計・A4印刷レイアウト・試験項目）を統合。UI表記を常用漢字中心に統一。QR 40mm設定と自前モーダルを反映 |
