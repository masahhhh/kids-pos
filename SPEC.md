# Kids POS 仕様書

| 項目 | 内容 |
| --- | --- |
| ドキュメント名 | Kids POS（おみせやさんレジ）仕様書 |
| バージョン | v1.1 |
| 最終更新 | 2026-08-02 |
| 状態 | 実機試用中（機能凍結） |

---

## 1. 目的とスコープ

子どもの「おみせやさんごっこ」で使用する、QRコード読み取り式の簡易レジアプリケーションである。商品に貼付したQRコードに税込価格を直接埋め込み、カメラで読み取って合計金額とおつりを算出する。

### 1.1 スコープ内

- 商品マスタ（名称・税込価格）の登録、編集、削除、一覧表示
- 商品QRコードの生成とA4普通紙への印刷
- カメラによるQRコード読み取りとカート投入
- 合計金額の算出、預り金入力、おつり算出
- 効果音・音量の設定

### 1.2 スコープ外

- 売上履歴の記録・集計、在庫管理、複数端末間のデータ同期
- 消費税計算（QRに税込価格を埋め込む方式のため、アプリ内では税計算を行わない）
- 会員管理、値引き、返品処理
- サーバサイド処理全般（バックエンドを持たない）

---

## 2. 動作環境

| 区分 | 要件 |
| --- | --- |
| 配信方式 | GitHub Pages（Publicリポジトリ、HTTPS自動付与） |
| 想定端末 | iPadOS / iOS の Safari（主）、Android Chrome、PCブラウザ（副） |
| 必須条件 | セキュアコンテキスト（HTTPS）。カメラAPIはHTTP環境では動作しない |
| 通信 | 初回読み込み時にCDNへの接続が必要。読み込み後の会計処理は端末内で完結する |
| 権限 | カメラ権限の許可が必要（レジ機能のみ） |

Service Workerは導入していないため、完全なオフライン動作は保証しない。イベント当日は事前にページを開いた状態を維持する運用とする。

---

## 3. システム構成

単一の `index.html` に、HTML・CSS・JavaScriptをすべて内包する静的Webアプリケーションである。フレームワークは使用しない（Vanilla JS）。

| 構成要素 | 採用技術 | 供給元 |
| --- | --- | --- |
| QRコード生成 | qrcode-generator 1.4.4 | `https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js` |
| QRコード読取 | jsQR 1.4.0 | `https://cdn.jsdelivr.net/npm/jsqr@1.4.0/dist/jsQR.js` |
| カメラ制御 | MediaDevices.getUserMedia（標準API） | ブラウザ組込 |
| 効果音 | Web Audio API（OscillatorNode） | ブラウザ組込 |
| データ保存 | localStorage | ブラウザ組込 |

### 3.1 読取ライブラリの選定経緯

当初は html5-qrcode（ZXing系）を採用したが、iOS環境で映像は表示されるにもかかわらずデコードが無言で失敗する事象が発生した。診断ページによる切り分けの結果、同一のカメラストリームに対して jsQR は正常にデコードできることを確認したため、jsQR＋自前フレーム処理へ全面移行した。

---

## 4. データ仕様

### 4.1 商品（localStorageキー `kidspos.products`）

JSON配列として保存する。要素の構造は以下のとおり。

| フィールド | 型 | 説明 |
| --- | --- | --- |
| `id` | string | 登録時刻を36進数化した内部ID |
| `name` | string | 商品名。1〜20文字。`\|` は使用不可 |
| `price` | number | 税込価格。0〜99999の整数 |

登録上限は30点とする。

### 4.2 設定（localStorageキー `kidspos.settings`）

| フィールド | 型 | 既定値 | 説明 |
| --- | --- | --- | --- |
| `tone` | string | `pi` | 効果音の種類。`pi` / `pipo` / `pon` / `charin` / `bell` / `none` |
| `vol` | string | `mid` | 音量。`low` / `mid` / `high` |
| `vibration` | boolean | `true` | バイブレーションの有効可否（iOSでは動作しない） |

### 4.3 カート（メモリ上のみ、永続化しない）

| フィールド | 型 | 説明 |
| --- | --- | --- |
| `name` | string | 商品名（QRから取得した値） |
| `price` | number | 税込価格（QRから取得した値） |
| `qty` | number | 数量。0以下になった時点で行を削除する |

同一行と判定する条件は「名称と価格の両方が一致すること」とする。商品IDはQRに含めないため、カートは商品マスタに依存しない。

---

## 5. QRコード仕様

| 項目 | 仕様 |
| --- | --- |
| データ形式 | `KP1\|商品名\|税込価格`（区切り文字はパイプ） |
| 例 | `KP1\|りんご\|120` |
| 文字コード | UTF-8（Byteモード。ライブラリの既定はマルチバイト非対応のため変換関数を明示指定） |
| 誤り訂正レベル | M |
| 型番 | 自動判定（データ長に応じて決定） |
| 描画形式 | SVG（`cellSize` 4、`margin` 8、スケーラブル出力） |
| 印刷サイズ | 28mm角 |

読み取り時は、要素数が3であること、先頭が `KP1` であること、価格が0以上の整数として解釈できることの3点を検証し、いずれかを満たさない場合はエラー音とともに「このQRコードは使えません」と表示する。

商品名が長いほど型番が上がり1マスが細くなるため、読み取り精度を優先する場合は短い名称を推奨する旨を登録画面に注記する。

---

## 6. 画面仕様

全体は単一ページ内のセクション切り替え（SPA方式）で構成し、ヘッダーには常時「ホーム」ボタンを配置する。

| 画面ID | 名称 | 主な機能 |
| --- | --- | --- |
| `scr-home` | ホーム | 各機能への遷移メニュー |
| `scr-list` | 商品一覧 | 一覧表示、並び替え、QRサムネイル表示切替、行タップで編集へ |
| `scr-products` | 商品の登録 | 新規登録、編集、削除、入力検証 |
| `scr-print` | QRコードの印刷 | 印刷プレビューと印刷実行 |
| `scr-register` | レジ | カメラ読取、カート操作、小計表示 |
| `scr-checkout` | お会計 | 預り金入力（キーパッド・定額ボタン）、おつり表示 |
| `scr-settings` | 設定 | 効果音、音量、バイブレーション、データ全削除 |

### 6.1 商品一覧

並び替えは登録順、名前順（日本語ロケール比較）、ねだん順の3種類を提供する。QRサムネイルは既定で非表示とし、ボタンで切り替える。表示件数は「登録数 n / 30点」の形式で示す。

### 6.2 レジ

カメラ映像の下に直近の読取結果を表示し、その下にカート、合計、操作ボタン（一覧から選ぶ／全部消す／お会計）を配置する。「一覧から選ぶ」は、QRが読めない場合の代替入力手段として商品マスタからのボタン選択を提供する。カートが空の間は「お会計」ボタンを無効化する。

### 6.3 お会計

預り金は3桁キーパッド（0〜9、00、C）と定額ボタン（100 / 500 / 1000 / 5000 / 10000／ちょうど）で入力する。入力桁数は7桁までに制限する。預り金が合計未満の場合は「お金が足りません」と表示し、「会計をおわる」ボタンを無効化する。

---

## 7. 計算仕様

合計金額は、カート内の各行について税込単価と数量の積を合算した $T=\sum_{i=1}^{n} p_i q_i$ で求める。おつりは預り金から合計を差し引いた $C = R - T$ とし、$C<0$ の場合は会計を確定させない。点数表示は $Q=\sum_{i=1}^{n} q_i$ とする。

| 変数 | 説明 |
| --- | --- |
| $n$ | カート内の明細行数 |
| $p_i$ | 第 $i$ 行の税込単価（円、整数） |
| $q_i$ | 第 $i$ 行の数量（正の整数） |
| $T$ | 合計金額（円） |
| $R$ | 預り金（円、0〜9999999） |
| $C$ | おつり（円） |
| $Q$ | 合計点数 |

金額はすべて整数で扱い、丸め処理および税計算は行わない。

---

## 8. 読み取り処理仕様

| 項目 | 仕様 |
| --- | --- |
| カメラ取得 | `facingMode: {ideal:'environment'}`、解像度は1920×1080を要求（`ideal`） |
| 走査間隔 | 150ms（`setInterval`） |
| 前処理 | video要素をcanvasへ描画。横幅1280pxを上限に縮小する |
| デコード | jsQRに `inversionAttempts: 'attemptBoth'` を指定 |
| 重複抑止 | 同一文字列は2000ms以内の再検出を無視する |
| 停止条件 | レジ以外の画面への遷移、会計画面への移行、タブの非表示化 |

### 8.1 フリーズ復旧

iOSでは `confirm` / `alert` の表示によりvideo要素が一時停止し、閉じても自動再開しない。この結果、直前のフレームを描画し続けて静止画状態となるため、以下の三重の復旧機構を実装する。

1. ダイアログを閉じた直後に再生を再開する（`resumeVideo()`）
2. video要素の `pause` / `ended` イベントを監視し、スキャン中であれば再開または再接続する
3. 走査ごとに `currentTime` の進行を監視し、約3秒（150ms × 20回）進まない場合はカメラを停止して再取得する

また、タブが再表示された際にレジ画面が表示中であればスキャンを自動再開する。

---

## 9. 効果音仕様

Web Audio APIのオシレータで合成する。音源ファイルは持たない。

| 種類 | 内容 |
| --- | --- |
| `pi` | 1200Hz、0.10秒、正弦波 |
| `pipo` | 988Hz→1319Hzの2音 |
| `pon` | 880Hz、0.30秒、三角波 |
| `charin` | 2093 / 3136 / 2637Hzの重ね合わせ（レジ風） |
| `bell` | 1568Hz＋3136Hz、減衰長め（チーン） |
| `none` | 無音 |
| エラー音 | 300Hz、0.30秒、鋸歯状波（共通・設定不可） |

音量は小0.06、中0.15、大0.30のゲイン値で切り替える。

### 9.1 iOSの無音対策

iOSではユーザー操作前のAudioContextが停止状態となり、またカメラ起動時に音声セッションが切り替わって無音化する事象がある。対策として、初回タップ時にAudioContextを生成・再開し無音バッファを再生してロックを解除するとともに、対応環境では `navigator.audioSession.type` に `playback` を宣言する。カメラ起動直後にも同処理を再実行する。本体が消音（サイレント）状態の場合は鳴らないため、設定画面に注意書きを掲示する。

---

## 10. 印刷仕様

| 項目 | 仕様 |
| --- | --- |
| 用紙 | A4普通紙、縦向き、余白10mm |
| レイアウト | 3列グリッド、1セル63.3mm × 55mm |
| 1ページあたり | 15点（3列 × 5行） |
| セル内容 | QRコード（28mm角）、商品名、価格、「税込」表記、切り取り用の破線 |
| プレビュー | 画面上は45%に縮小表示し、印刷時は等倍とする |

切り取り線を紙面に出力するには、印刷ダイアログで「背景のグラフィック」を有効にする必要がある。

---

## 11. 非機能要件・制約事項

- ソースコードはGitHub Pagesの仕様上公開されるため、APIキー等の機密情報および個人情報を一切含めない。
- 商品データは端末のlocalStorageにのみ保存され、外部へ送信されない。ブラウザのデータ削除により消失する。
- 対象年齢は小学校高学年を想定し、画面表記は常用漢字を中心とした平易な日本語とする。
- バイブレーションはiOS/iPadOSのSafariが `navigator.vibrate` を実装していないため動作しない。設定項目は他環境向けに残置する。
- 商品名に `|` を含めることはできない（QRの区切り文字と衝突するため）。

---

## 12. 試験項目（受け入れ基準）

| # | 確認内容 | 期待結果 |
| --- | --- | --- |
| 1 | 商品を登録する | 一覧・印刷プレビューにQR付きで表示される |
| 2 | 20文字超・空文字・負の価格を入力する | エラーメッセージが表示され登録されない |
| 3 | 30点登録後にさらに登録する | 上限エラーが表示される |
| 4 | 印刷プレビューを実行する | A4に3列×5行で配置され、QRが欠落しない |
| 5 | 印刷したQRを読み取る | カートに追加され、効果音が鳴る |
| 6 | 同じQRを連続してかざす | 2秒以内は重複追加されない |
| 7 | 「全部消す」でOK／キャンセルを押す | 映像が静止せず読み取りを継続できる |
| 8 | 他アプリへ移動して戻る | レジ画面ならカメラが自動再開する |
| 9 | 預り金が合計未満 | 「お金が足りません」と表示され確定できない |
| 10 | 会計を完了する | おつりが表示され、カートが空になり読取が再開する |

---

## 13. 今後の拡張候補

- 売上履歴の記録と当日集計の表示
- PWA化（Service Worker導入によるオフライン起動、CDN依存の解消）
- 商品データのバックアップ・復元（JSONの書き出しと読み込み）
- 印刷QRサイズの選択（読み取りが不安定な環境向けに40mm角を追加）
- HTMLベースの確認ダイアログへの置き換え（ネイティブダイアログ依存の排除）
